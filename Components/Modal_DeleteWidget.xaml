<UserControl x:Class="Widger.Components.Modal_DeleteWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Widger.Components"
             mc:Ignorable="d" 
             d:Background="#ffffff"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Style="{DynamicResource LabelHeading}" Content="Are you sure you want to delete it?" />
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Text="The deleted widget cannot be restored" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Delete" Background="{DynamicResource ErrorColor}" Click="Delete_Click" />
            <Button Content="Cancel" Margin="30,0,0,0" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</UserControl>
