<UserControl x:Class="Widger.Components.Toast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Widger.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border x:Name="RootBorder" Style="{DynamicResource Toast}" Background="{DynamicResource ButtonColor}">
        <Label x:Name="ToastMessage" Content="Toast Text" Foreground="#FFFFFF" FontSize="10" Padding="5" />
    </Border>
</UserControl>
