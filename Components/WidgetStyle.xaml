<Window x:Class="Widger.Components.WidgetStyle"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:Widger.Components"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        mc:Ignorable="d"
        MinHeight="300" MinWidth="400"
        Title="WidgetStyle" Height="300" Width="400">

    <Border x:Name="WinStyle" Style="{DynamicResource Widget}" MouseDown="WindowDrag_MouseDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Style="{DynamicResource LabelHeading}" Content="Widget style" Margin="0,0,0,10" />

            <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Stretch">

                <!-- Background color -->
                <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                    <Label Content="Background" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="BgHexTextBox" VerticalContentAlignment="Center" Text="#FFFFFF" Margin="0,0,8,0"/>

                        <ToggleButton x:Name="BgPickButton" Grid.Column="1" Content="..."/>

                        <Popup PlacementTarget="{Binding ElementName=BgPickButton}" Placement="Bottom" StaysOpen="False" IsOpen="{Binding IsChecked, ElementName=BgPickButton}">
                            <Border Background="White" BorderBrush="#66000000" BorderThickness="1" Padding="10">
                                <StackPanel>
                                    <xctk:ColorCanvas x:Name="BgCanvas"
                                                      Width="300" Height="180"
                                                      SelectedColorChanged="Canvas_SelectedColorChanged"/>
                                    <Button Margin="0,10,0,0" Content="OK" Click="BgOk_Click"/>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>
                </StackPanel>

                <!-- Text color -->
                <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
                    <Label Content="Text" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="TextHexTextBox" VerticalContentAlignment="Center" Text="#2D2E33" Margin="0,0,8,0"/>

                        <ToggleButton x:Name="TextPickButton" Grid.Column="1" Content="..."/>

                        <Popup PlacementTarget="{Binding ElementName=TextPickButton}" Placement="Bottom" StaysOpen="False" IsOpen="{Binding IsChecked, ElementName=TextPickButton}">
                            <Border Background="White" BorderBrush="#66000000" BorderThickness="1" Padding="10">
                                <StackPanel>
                                    <xctk:ColorCanvas x:Name="TextCanvas"
                                                      Width="300" Height="180"
                                                      SelectedColorChanged="Canvas_SelectedColorChanged"/>
                                    <Button Margin="0,10,0,0" Height="28" Content="OK" Click="TextOk_Click"/>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>
                </StackPanel>

            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Save" Click="Save_Click" />
                <Button Content="Cancel" Background="{DynamicResource ErrorColor}" Margin="30,0,0,0" Click="Cancel_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>